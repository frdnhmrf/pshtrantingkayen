import React from 'react';
import {SafeAreaView, View} from 'react-native';
import MainImageView from '../../../components/molecules/MainImageView';

const Views = () => {
  return (
    <SafeAreaView>
      <View>
        <MainImageView />
      </View>
    </SafeAreaView>
  );
};

export default Views;
