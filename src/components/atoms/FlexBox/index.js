import React from 'react';
import {View} from 'react-native';

const FlexBox = () => {
  return (
    <View
      style={{
        borderBottomColor: '#000000',
        opacity: 30 / 100,
        borderBottomWidth: 1,
        marginVertical: 10,
      }}
    />
  );
};

export default FlexBox;
